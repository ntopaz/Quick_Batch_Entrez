Quick_Batch_Entrez v1.0

Allows the user to input a fasta or clustal file and quickly run NCBI's Batch Entrez. This will retrieve all the 
sequences found in the input file, and return them in unalligned fasta format. 
